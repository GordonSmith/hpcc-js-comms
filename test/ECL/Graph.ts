import { Graph } from "../../src/ECL/Graph";
import { describe, expect, it } from "../lib";

describe.skip("Graph", function () {
    it("basic", function () {
        expect(Graph).is.function;
    });
});
